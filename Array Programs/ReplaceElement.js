// Replace an item from an array in JavaScript :

// Using Array Indexing :
let arr = [20, 30, 40, 50]

const index = 3
const newVal = 70

arr[index] = newVal
console.log(arr)

// Using Splice method :

const list = ['jan', 'feb', 'April', 'May']

list.splice(2, 2, 'march')

list.splice(3, 3, 'April')

console.log(list)
